@model skudatabase.domain.Models.SKUPartConfig

@{
    ViewData["Title"] = "Delete SKUPart Config";
}

<h1>Delete SKUPart Config</h1>

<h4>SKUPart Config</h4>
<hr />
<div>
    <h4>Are you sure you want to delete this?</h4>
    <dl class="row">
        <dt class="col-sm-2">
            Generic Name
        </dt>
        <dd class="col-sm-10">
            @Model.GenericName
        </dd>
        <dt class="col-sm-2">
            SKUConfigId
        </dt>
        <dd class="col-sm-10">
            @Model.SKUConfigId
        </dd>
        <dt class="col-sm-2">
            Status
        </dt>
        <dd class="col-sm-10">
            @Model.Status
        </dd>
    </dl>

    <form asp-action="DeleteConfirmed">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>